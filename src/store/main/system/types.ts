export interface ISystemState {
    userTotal: number
    userList: any[]
    swiperList: any[]
    swiperTotal: number
    sortList: any[]
    sortTotal: number
}

export interface IPageTableForm {
    size: number
    offset: number
}